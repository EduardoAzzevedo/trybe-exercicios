function biggestWord(array) {
    let bigestWord = [];
    for (let key in array) {
        if (array[bigestWord] < array[index]) {
            bigestWord.push([key]);
        }
    return bigestWord;
    }
}
console.log(biggestWord(['José', 'Lucas', 'Nádia', 'Fernanda', 'Cairo', 'Joana']));